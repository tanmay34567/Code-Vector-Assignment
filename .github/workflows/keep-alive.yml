name: Keep Alive Ping

on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: |
          curl -I https://code-vector-assignment.onrender.com/api/ping
